version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    # Eğer varsa ports, environment vb. ayarların aynen kalsın, sadece üstteki build kısmını düzelt.

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    # Frontend'e ait diğer ayarların aynen kalsın.
