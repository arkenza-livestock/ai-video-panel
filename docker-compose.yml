version: '3.8'

services:
  app:
    build: .
    ports:
      - "3012:3012"
    environment:
      - PORT=3012
    volumes:
      - ./backend:/app/backend
      - ./frontend/dist:/app/backend/static
