version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3009:8000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3010:80"
    depends_on:
      - backend
    restart: unless-stopped
